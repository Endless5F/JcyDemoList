<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.android.architecture">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/> <!-- Declare features -->
    <uses-feature android:name="android.hardware.camera"/>

    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:replace="android:icon"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".demolist.workmanager.WorkManagerActivity"/>
        <activity android:name=".demolist.paging.PagingActivity" />
        <activity android:name=".demolist.paging.basic.BasicUsageActivity" />
        <activity android:name=".demolist.paging.header_simple.HeaderSimpleActivity" />
        <activity android:name=".demolist.paging.header_proxy.HeaderProxyActivity" />
        <activity android:name=".demolist.room.RoomActivity" />
        <activity android:name=".demolist.databinding.DataBindingActivity" />
        <activity android:name=".demolist.livedata.LiveDataActivity" />
        <activity android:name=".demolist.viewmodel.ViewModelActivity" />
        <activity android:name=".demolist.navigation.NavigationActivity" />
        <activity android:name=".demolist.lifecycle.LifecycleActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>